import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as IconButtonStories from './IconButton.stories';

<Meta of={IconButtonStories} />

# IconButton

The `IconButton` component is a thin wrapper around [MUI IconButton](https://mui.com/material-ui/react-button/#icon-button), inheriting all its props and behaviour while extending it with a `variant` prop and custom `extraLarge` size - themed via the Tonic design system.

---

## Usage

```tsx
import { IconButton } from '@bmi/mui-tonic-components';

<IconButton color="primary" size="medium">
  <MyIcon />
</IconButton>
```

---

## Variants

Tonic IconButton supports two visual variants:

### Default
Transparent background with a colored icon. Use for low-emphasis icon actions.

<Canvas of={IconButtonStories.Default} />
<Controls of={IconButtonStories.Default} />

---

### Contained
Filled circular background using `color.main`, white icon. Use for high-emphasis icon actions.

<Canvas of={IconButtonStories.Contained} />

---

### Both Variants Side-by-Side

<Canvas of={IconButtonStories.BothVariants} />

---

## Disabled State

Both variants support a disabled state. Interaction is blocked and the button renders with reduced opacity.

<Canvas of={IconButtonStories.Disabled} />
<Canvas of={IconButtonStories.DisabledContained} />

---

## Sizes

Tonic adds an `extraLarge` size on top of MUI's defaults. All four sizes use a 24 px icon; only the padding differs.

<table>
  <thead>
    <tr><th>Size</th><th>Total size</th><th>Padding</th></tr>
  </thead>
  <tbody>
    <tr><td><code>small</code></td><td>32 px</td><td>4 px</td></tr>
    <tr><td><code>medium</code></td><td>36 px</td><td>6 px</td></tr>
    <tr><td><code>large</code></td><td>40 px</td><td>8 px</td></tr>
    <tr><td><code>extraLarge</code></td><td>48 px</td><td>12 px</td></tr>
  </tbody>
</table>

<Canvas of={IconButtonStories.AllSizes} />

---

## Colors

All MUI theme colors are supported for both variants.

### Default (Transparent)

<Canvas of={IconButtonStories.AllColors} />

### Contained (Filled)

<Canvas of={IconButtonStories.AllColorsContained} />

---

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>variant</code></td>
      <td><code>'default' | 'contained'</code></td>
      <td><code>'default'</code></td>
      <td>Visual style - transparent or filled circular background</td>
    </tr>
    <tr>
      <td><code>color</code></td>
      <td><code>'primary' | 'secondary' | 'error' | 'warning' | 'info' | 'success' | 'default' | 'inherit'</code></td>
      <td><code>'primary'</code></td>
      <td>Theme colour applied to the icon (and background in contained variant)</td>
    </tr>
    <tr>
      <td><code>size</code></td>
      <td><code>'small' | 'medium' | 'large' | 'extraLarge'</code></td>
      <td><code>'medium'</code></td>
      <td>Controls overall button size via padding (icon stays 24 px)</td>
    </tr>
    <tr>
      <td><code>disabled</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Disables interaction and applies reduced opacity</td>
    </tr>
    <tr>
      <td><code>onClick</code></td>
      <td><code>() =&gt; void</code></td>
      <td>-</td>
      <td>Click handler</td>
    </tr>
    <tr>
      <td><code>children</code></td>
      <td><code>ReactNode</code></td>
      <td>-</td>
      <td>Icon element to render inside the button</td>
    </tr>
  </tbody>
</table>

> All other [MUI IconButtonProps](https://mui.com/material-ui/api/icon-button/) are also accepted.
