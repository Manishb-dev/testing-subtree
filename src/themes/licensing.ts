import { createTheme } from '@mui/material/styles';
import { tonicLight, tonicDark } from './tonic';

export const licensingLight = createTheme(tonicLight);

export const licensingDark = createTheme(tonicDark);
